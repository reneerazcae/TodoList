spring.application.name=todo-list
# Server Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/todo_db?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=todolist_user
spring.datasource.password=reneerazcae
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# (Optional) Lombok Configuration
# If using Lombok, ensure annotation processing is enabled
